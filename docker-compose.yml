services:
  gns3server:
    image: gns3/gns3-server:latest
    container_name: gns3server
    privileged: true
    ports:
      - "3080:3080"
    volumes:
      - ./ios-images:/root/GNS3/images/IOS
      - ./gns3-data:/root/GNS3
    restart: unless-stopped
